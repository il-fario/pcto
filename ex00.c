#include <stdio.h>

int main ()
{
   float o = 28; //flag ; %f
   char e = 'E'; //flag ; %c
   char l = 'L'; //flag ; %c
   int q = 11; //flag ; %c
   printf("ho creato una variabile float con valore %f, il primo carattere %c è uguale al secondo carattere %c, ho creato una terza variabile int con valore %c/n", o, e, l, q);
}